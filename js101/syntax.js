/*  Declarations of variables:
    var - declares a global/function-scoped variable
    let - declares a local variable
    const - declares an unchangeable function variable
*/
const str = "A string";
const num = 3;
const flt = 3.14;
const bool = true;
const list = ["Item 1", 2, "Three", 3.14, true, []];  // List (not data specific)
const NULL = null; // has nothing in it
const UNDEFINED = undefined; // Holds no value
const func = function() { return 24; }
const obj = {
    'name': 'Ian',
    'profession': 'software developer'
}
